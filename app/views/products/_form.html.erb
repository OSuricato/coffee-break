<%= simple_form_for([ @user, @product ]) do |f| %>


  <%= f.input :name %>

  <%= f.input :description %>

  <%= f.input :region %>

  <%= f.input :roast_type, collection: ['grain', 'coarse', 'thin'] %>

  <%= f.input :weight, collection: ["250g", "500g", "1000g"] %>

  <%= f.input :price %>


  <div class="d-grid">
    <%= f.button :submit, class: 'btn btn-primary' %>
  </div>
<% end %>
